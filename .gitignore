# Working artifacts produced by /cscale runs. Never publish.
runs/
**/runs/
.claude/cscale-telemetry.json

# Logs and OS junk
*.log
.DS_Store
Thumbs.db

# Secrets (defense in depth - the skill itself ships none)
.env
.env.local
.env.*.local
*.pem
*.key

# Node tooling (in case anyone scripts around the installers)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor caches
.vscode/
.idea/
*.swp
